# 产品需求文档 (PRD): Dota 2 职业级录像分析工具 ("True Sight")

## 1. 项目简介
**目标**: 为 Dota 2 职业战队打造一款可执行的桌面端录像分析应用。该工具旨在通过提供比公共数据网站（如 Dotabuff）更深度的洞察，特别是专注于**位置数据**、**视野效率**以及基于 **AI 的对手模式识别**，从而辅助战队制定战术。

## 2. 用户画像 (User Personas)
- **分析师 (Analyst)**: 负责上传训练赛/比赛录像，标记关键时间点，生成关于对手习惯的深度报告。
- **教练 (Coach)**: 使用 2D 地图回放功能与队员一起复盘比赛，通过在地图上画线来讲解战术移动。
- **职业选手 (Player)**: 检查特定的个人指标（例如：“我的刷钱效率是否达到了 一线 Carry 的水平？”）并研究对位选手的习惯。

## 3. 项目与分期规划

### 第一阶段: MVP (基础功能)
- **本地桌面应用**: 离线优先，稳定，数据安全，无需上传大文件。
- **录像解析**: 高性能解析 `.dem` 录像文件。
- **比赛仪表盘**:
  - 记分板 (KDA, 财产总和, 正反补)。
  - 趋势图表 (金钱差, 经验差, 胜率曲线)。
- **2D 地图回放**:
  - 可交互的 Dota 2 地图俯视图。
  - 时间轴拖动条。
  - 显示英雄、建筑、信使和守卫 (眼) 的实时图标移动。

### 第二阶段: AI 与进阶模块 (核心差异化)
- **习惯分析 (AI)**:
  - **眼位聚类分析**: “LGD 战队在天辉方 10-20 分钟通常把眼插在哪里？”
  - **开雾路径**: 识别并展示常见的开雾游走路线。
- **选手/战队数据库**:
  - 创建本地数据库，按“选手”或“战队”归档录像。
  - 支持“多录像聚合分析”：同时选中 10 场比赛，生成综合热力图。
- **性能对标**:
  - 将选手的关键指标（如“补刀/分钟”，“支援响应时间”）与数据库中的顶级职业局数据进行对比。

## 4. 功能需求 (Functional Requirements)

### 4.1. 仪表盘 (MVP)
- **FR-01**: 用户可以拖拽 `.dem` 文件进入，或**通过内置搜索下载录像**。
- **FR-02**: 显示比赛摘要头部信息（获胜方、持续时间、比赛 ID）。
- **FR-03**: 显示包含财产总和、KDA、关键装备（带时间戳）的选手列表。
- **FR-04**: 可交互的金钱/经验差曲线图。
- **FR-04-B (录像中心_新增)**: 
  - 集成 OpenDota/Stratz API，输入 Match ID 自动获取并下载 Replay。
  - 自动解压并归档到本地 `demos/` 目录。

### 4.2. 2D 地图回放 (MVP)
- **FR-05**: 渲染 Dota 2 地图背景（需适配当前 7.xx 版本地图）。
- **FR-06**: 根据每帧坐标数据 (x, y) 渲染移动的英雄图标。
- **FR-07**: 显示守卫图标（假眼/真眼）及其存活时间计时器。
- **FR-08**: 战争迷雾切换开关（显示全图、仅显示天辉视野、仅显示夜魇视野）。

### 4.3. AI 与模式识别 (Phase 2)
- **FR-09 (眼位习惯)**: 聚合特定战队/选手的多场录像眼位数据。通过“热力图”或“聚类点”形式展示。
- **FR-10 (游走习惯)**: 分析辅助选手的游走时机（例如：“平均游走到中路的时间：5分30秒”）。
- **FR-11 (BP 预测)**: (可选) 基于对手历史选取率的简单概率模型。
- **FR-12 (选手画像_新增)**: 
  - 选中某选手 ID（如 Ame），自动扫描本地所有含该选手的录像。
  - 生成“行为指纹”报告（常去刷钱点、平均 BKB 时间）。

## 5. 非功能需求 (Non-Functional Requirements)
- **性能**: 标准职业比赛录像（约45分钟）必须在现代笔记本电脑上于 **30秒内** 完成解析。
- **隐私**: 所有数据处理默认在**本地**进行。除非用户明确分享，否则不上传数据。
- **可用性**: 默认深色模式 (Dark Mode)。UI 信息密度需适配高分辨率显示器。

## 6. 技术架构

### 6.1. 技术栈选择
- **前端 (UI & Map)**: **Electron** (React + TypeScript)。
  - *理由*: 跨平台桌面应用的最佳选择。利用 **PixiJS** 或 Canvas API 可实现高性能的 2D 地图渲染（处理数千个实体）。
- **解析引擎**: **Go (Dotabuff Manta)** 或 **Rust (Claritas)**。
  - *理由*: Python 在逐帧解析二进制 Protobuf 文件时速度过慢。为了达到“30秒解析”的目标，必须使用编译型语言。解析器将输出结构化的 JSON/CSV 或 Parquet 文件。
- **分析核心 & AI**: **Python**。
  - *理由*: 数据科学领域的绝对霸主 (**Pandas** 用于统计，**Scikit-learn** 用于聚类算法，**PyTorch** 用于复杂模型)。
  - *集成方式*: Electron 启动一个 Python 子进程 (Sidecar)。Electron 发送“解析指令”，Python 调用 Go 解析器，读取产物，计算统计数据，最后返回 JSON 给 Electron。

### 6.2. AI 可行性分析
- **数据源**: 解析器提取每一帧 (Tick) 的数据，包含所有 10 名英雄 + 守卫的 (x,y) 坐标。
- **训练/推理流程**:
  - **输入**: 指定选手的 20 场录像文件夹。
  - **处理**:
    1. 筛选出“侦查守卫 (Observer Ward)”实体。
    2. 归一化处理 (按游戏时间 0-10分钟, 10-20分钟 分组)。
    3. 算法: 使用 **DBSCAN** (基于密度的空间聚类算法) 识别眼位“簇”。
  - **输出**: 高频眼位坐标列表及权重。
- **结论**: **高可行性**。这属于统计学模式识别问题，而非生成式 AI，使用标准的机器学习技术即可完美解决。

## 7. 下一步行动
1.  初始化 **Electron** 项目结构。
2.  搭建 **Python** 虚拟环境及基础脚本。
3.  使用 **Manta (Go)** 解析器测试解析一个示例 `.dem` 文件。
